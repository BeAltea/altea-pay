# ═══════════════════════════════════════════════════════════════════════════════
# Altea Pay - Environment Variables
# ═══════════════════════════════════════════════════════════════════════════════
#
# Copy this file to .env and fill in the values
# For local development, run: ./start-local.sh
#
# ═══════════════════════════════════════════════════════════════════════════════

# ──── Local Docker PostgreSQL ────
# These are used by docker-compose for the local PostgreSQL container
DATABASE_URL=postgresql://altea:altea@localhost:5432/alteapay
POSTGRES_USER=altea
POSTGRES_PASSWORD=altea
POSTGRES_DB=alteapay

# ──── NextAuth ────
# Required for authentication
# Generate secret with: openssl rand -base64 32
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=
AUTH_SECRET=
AUTH_TRUST_HOST=true

# ──── Application ────
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ──── Supabase Database Connection (for data migration) ────
# These are used by start-local.sh to migrate data from Supabase
# Get these from your Supabase project: Settings > Database > Connection string
#
# Example connection string: postgres://postgres.[ref]:[password]@[host]:5432/postgres
# Split it into these components:
SUPABASE_DB_HOST=aws-1-sa-east-1.pooler.supabase.com
SUPABASE_DB_PORT=5432
SUPABASE_DB_NAME=postgres
SUPABASE_DB_USER=postgres.your-project-ref
SUPABASE_DB_PASSWORD=your-database-password

# ──── Supabase API (Legacy - for reference only) ────
# These were used by the old Supabase client SDK
# No longer required after migration to Drizzle ORM
SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=

# ──── Email (Resend) ────
# For sending transactional emails
# Get API key from: https://resend.com
RESEND_API_KEY=

# ──── SMS (Twilio) ────
# For sending SMS notifications
# Get credentials from: https://www.twilio.com/console
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
TWILIO_MESSAGING_SERVICE_SID=

# ──── Credit Analysis (Assertiva) ────
# For credit scoring and analysis
# Get credentials from: https://www.assertivasolucoes.com.br
ASSERTIVA_CLIENT_ID=
ASSERTIVA_CLIENT_SECRET=
ASSERTIVA_BASE_URL=https://api.assertivasolucoes.com.br
ASSERTIVA_CALLBACK_URL=
ASSERTIVA_WEBHOOK_SECRET=

# ──── Payments (ASAAS) ────
# For payment processing and boletos
# Get API key from: https://www.asaas.com
ASAAS_API_KEY=

# ──── Cron Security ────
# Secret for authenticating cron job requests
# Generate with: openssl rand -base64 32
CRON_SECRET=

# ──── Government Portal ────
# For government transparency portal integration (optional)
PORTAL_TRANSPARENCIA_API_KEY=
